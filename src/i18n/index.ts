import { fr } from "./fr";

/**
 * Point d'entrée unique des chaînes de l'interface.
 * Pour ajouter l'anglais : créer en.ts avec la même forme que fr.ts, puis
 * remplacer la valeur exportée ici. Aucun composant n'a besoin d'être modifié.
 */
export const t = fr;

export type Traductions = typeof fr;
